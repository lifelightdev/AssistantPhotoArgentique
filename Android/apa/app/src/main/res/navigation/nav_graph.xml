<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/permissions_fragment">

    <fragment
        android:id="@+id/permissions_fragment"
        android:name="life.light.android.apa.fragments.PermissionsFragment"
        android:label="PermissionsFragment" >
        <action
            android:id="@+id/action_permissions_to_accueil"
            app:destination="@id/accueil_fragment"
            app:popUpTo="@id/permissions_fragment"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/accueil_fragment"
        android:name="life.light.android.apa.fragments.AccueilFragment"
        android:label="AccueilFragment" >
        <action
            android:id="@+id/action_accueil_to_camera"
            app:destination="@id/camera_fragment" />
        <action
            android:id="@+id/action_accueil_to_permissions"
            app:destination="@id/permissions_fragment"
            app:popUpTo="@id/accueil_fragment"
            app:popUpToInclusive="true"/>
    </fragment>

    <fragment
        android:id="@+id/camera_fragment"
        android:name="life.light.android.apa.fragments.CameraFragment"
        android:label="CameraFragment" >
        <action
            android:id="@+id/action_camera_to_gallery"
            app:destination="@id/gallery_fragment" />
        <action
            android:id="@+id/action_camera_to_accueil"
            app:destination="@id/accueil_fragment"
            app:popUpTo="@id/camera_fragment"
            app:popUpToInclusive="true"/>
        <argument
            android:name="appareil_photo"
            app:argType="string"
            android:defaultValue="Aucun" />
    </fragment>

    <fragment
        android:id="@+id/gallery_fragment"
        android:name="life.light.android.apa.fragments.GalleryFragment"
        android:label="GalleryFragment" >
        <argument
            android:name="root_directory"
            app:argType="string" />
    </fragment>

</navigation>